FROM node:20-alpine
WORKDIR /app
COPY package.json package-lock.json vite.config.js index.html ./
COPY src ./src
COPY public ./public
RUN npm ci || npm i
EXPOSE 5173
CMD ["npm", "run", "dev", "--", "--host", "0.0.0.0"]
